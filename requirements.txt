fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9       # para Form() y UploadFile
pydantic[email]==2.8.2        # EmailStr y validaciones
pydantic-settings==2.4.0      # BaseSettings
python-jose[cryptography]==3.3.0  # JWT
passlib[bcrypt]==1.7.4        # hashing de contraseñas
bcrypt==4.0.1                 # pin: passlib 1.7.4 no soporta bcrypt >=4.1
psycopg2-binary==2.9.9        # PostgreSQL
python-dotenv==1.0.1          # carga el .env
opencv-python-headless==4.8.1.78  # procesamiento de video (sin libs gráficas del SO, para hosts Linux en la nube)
numpy==1.26.4                  # pin: ultralytics 8.2.91 exige numpy<2.0.0
pandas==2.2.2                  # pin: evita resolver un pandas 3.x incompatible
# Nota: pandas 2.2.2 + numpy 1.26.4 tiene un bug binario conocido en un DataFrame
# interno de ultralytics (AutoBackend._model_type). Se evita con un parche en
# detector/yolo_detector.py que reemplaza esa función para no usar pandas ahí.
ultralytics==8.2.91           # YOLOv8

# torch/torchvision NO están aquí a propósito: se instalan aparte (build CPU explícita)
# mediante el Build Command de Railway, antes de este archivo (ver instrucciones de despliegue).
# Para desarrollo local con GPU NVIDIA, instalar la build CUDA por separado:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
